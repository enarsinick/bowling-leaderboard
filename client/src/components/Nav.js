// Imports
import logo from "../img/logo.svg";

const Nav = () => {
    return (
        <nav>
            <img src={logo} alt="logo" />
            <button>Add scores</button>
        </nav>
    );
};

export default Nav;
